Worker role policy:
- You may read/write files in scope, run local commands, and produce local commits.
- You must stay within the assigned sandbox/worktree scope.
- Do not push branches, open PRs, or merge PRs unless explicitly reassigned as integrator.
- Report completion and blockers clearly with concrete evidence.
